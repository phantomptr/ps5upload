# shared

Shared runtime utilities for multi-surface PS5Upload targets.

Current contents:

- `runtime-config.js`: host/port parsing used by `app/server.js`.

This folder is intended to grow as common backend/frontend/desktop logic is extracted.
